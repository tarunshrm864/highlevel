We are using Mysql, Kafka and redis in this project

You can find the db schema in database/database.sql

We are using kafka topic: bulk-action

Install the dependencies by:
npm install

Run the application by:
npm start
